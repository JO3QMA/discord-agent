# 定着レビューは会話セッションから切り離す

定着レビューを会話セッションから切り離し、直近ターンのテキストだけを渡す使い捨てエージェントで実行する。会話全文・ツール結果・作業ディレクトリ変更はレビューから見えなくなる劣化を受け入れる（複数ターンにまたがる Skill 抽出は弱くなる）。MCP は builtin の memory-skills のみ、Operator は env でプロセス単位に渡す（active-operator ファイルの配置単位競合を避ける）。毎ターン Agent.create するレイテンシ増も受け入れる。
